type Annotation = {
	id?: 'REASONING_STEPS' | 'SOURCES';
	other?: string[];
};

type ReasoningStepsAnnotation = {
	id: 'REASONING_STEPS';
	other: string[];
};

type SourcesAnnotation = {
	id: 'SOURCES';
	other: string[];
};
