---
name: testing
description: asdf
---
# testing

## Search Knowledge Base

You can search this knowledge base using the `search` command. Example:

```bash
compeer search "testing" "testing" "Frontend design specification"
```

## Add to Knowledge Base

If you need to remember something to be searched later, use the `capture` command.

Example of capturing text:

```bash
compeer capture "testing" "testing" "This project is for UI development"
```

Example of capturing a website:

```bash
compeer capture "testing" "testing" "https://compeer.ai"
```

Example of capturing a data payload:

```bash
compeer capture  "testing" "testing" "{'hello': 'world'}"
```